export const input = {
  left: false,
  right: false,
  up: false,
  down: false,
  fire: false
};

// mobile touch controls were removed — input flags are still exported and
// controlled by keyboard events in main.js
